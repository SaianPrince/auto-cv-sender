# Sender Configuration (Gmail credentials)
EMAIL_USER=your.email@gmail.com
EMAIL_PASS=your-16-character-app-password

# Running Configuration
CV_PATH=C:/path/to/your/CV.pdf
DELAY_SEC=45
# DRY_RUN can be controlled directly inside the script or set as an env var if customized.
